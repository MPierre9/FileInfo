language: python
python:
  - "2.7"
cache: pip
install:
  - pip install -r requirements.txt
  - pip install pytest
  - pip install pep8
script:
  - find . -name \*.py -exec pep8 --ignore=E402 {} +
  - echo $num_errors_before
  - python tests.py
